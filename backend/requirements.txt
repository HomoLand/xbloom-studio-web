# Production / release install — no sibling checkout assumption.
# Install from the backend/ directory:
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1   # or: source .venv/bin/activate
#   pip install -r requirements.txt
#
# Pins xbloom-studio-core to the published GitHub Release v1.2.0 wheel with
# SHA-256 integrity. Do not use this file for local core development; use
# requirements-dev.txt instead (editable core, no release wheel).
#
# Knowledge assets are separate: download knowledge-1.2.0.zip from the same
# release, verify its published hash, extract it, and set XBLOOM_ASSETS_DIR
# to the extracted bundle's assets directory.

xbloom-studio-core @ https://github.com/HomoLand/xbloom-studio-brew/releases/download/v1.2.0/xbloom_studio_core-1.2.0-py3-none-any.whl#sha256=9a90c781e4a9bd756f60103ef4f51d1966b83ef5be466c5cf88e7d8264b5b918
-r requirements-runtime.txt
